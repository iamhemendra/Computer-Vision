# Computer-Vision
We developed an automated ROI tracking system using template matching and adaptive re-detection strategies to track objects across video frames and generate structured outputs for downstream processing
